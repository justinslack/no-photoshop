# No thanks to Photoshop

## How to Use?

1. Clone this repo 
2. Install dependencies by running `npm i` 
3. Run a copy of the project locally using `npm run serve` to see your changes locally.
4. Run `npm run build` which will build your website in the `_site` folder which you can deploy.

## Or just use Netlify
